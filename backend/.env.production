# Production environment configuration for FoyerGPT Backend

# Database Configuration
DB_NAME=oskour
DB_USER=GJV
DB_PASSWORD=
DB_HOST=localhost
DB_PORT=5432

# Backend Configuration
BACKEND_HOST=0.0.0.0
BACKEND_PORT=8066
BACKEND_URL=http://foyergpt.lefoyer.lu:8066

# Frontend Configuration
FRONTEND_URL=http://foyergpt.lefoyer.lu:8067

# CORS Origins (including all necessary domains)
CORS_ORIGINS=http://foyergpt.lefoyer.lu:8067,http://foyergpt.lefoyer.lu:8066,http://localhost:8067,http://localhost:8066

# LLM Mode Configuration
LLM_MODE=api

# Mistral API Configuration
MISTRAL_API_KEY=${MISTRAL_API_KEY}
MISTRAL_MODEL=mistral-small-latest
VISION_MODEL=pixtral-12b-2024-09-24
# VISION_API_URL=https://api.mistral.ai/v1/chat/completions
# VISION_API_KEY=${MISTRAL_API_KEY}

# Security
JWT_SECRET_KEY=${JWT_SECRET_KEY}

# Redis Configuration
REDIS_URL=redis://localhost:6379/0

# Session Configuration
SESSION_EXPIRE_HOURS=24

# Storage Configuration
STORAGE_PATH=./storage
MAX_FILE_SIZE_MB=10
MAX_DOCUMENTS_PER_AGENT=10
MAX_DOCUMENTS_PER_CHAT=5

# PDF Processing
PDF_USE_VISION_THRESHOLD=100
PDF_MAX_PAGES_VISION=10
