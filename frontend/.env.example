# URL de l'API backend (FastAPI)
# Pour développement local: http://localhost:8061
# Pour production: remplacer par l'URL publique du backend
VITE_API_URL=http://localhost:8061

# URL du WebSocket (généralement même que l'API)
VITE_WEBSOCKET_URL=http://localhost:8061

# Port sur lequel le frontend doit écouter
VITE_PORT=8066

# Host sur lequel le frontend doit écouter
# 0.0.0.0 permet l'accès depuis n'importe quelle interface réseau
VITE_HOST=0.0.0.0