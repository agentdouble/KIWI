# URL du backend (HTTP + WebSocket). Doit correspondre à BACKEND_URL dans backend/.env
VITE_BACKEND_URL=http://localhost:8077

# Alias conservés pour compatibilité
VITE_API_URL=http://localhost:8077
VITE_WEBSOCKET_URL=http://localhost:8077

# URL complète du frontend (utilisée par Vite pour déterminer le port et les CORS)
VITE_FRONTEND_URL=http://localhost:8091

# Host sur lequel le frontend doit écouter (true ou 0.0.0.0 pour réseau local)
VITE_HOST=0.0.0.0
